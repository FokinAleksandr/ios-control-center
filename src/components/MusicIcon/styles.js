import { StyleSheet } from 'react-native';
import { horizontalUnits } from '../../utils/scaling';

export default StyleSheet.create({
  iconImage: {
    width: horizontalUnits(24),
    height: horizontalUnits(24),
  },
});
